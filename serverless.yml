service: test-app
provider:
  name: aws
  runtime: nodejs18.x
  region: ap-northeast-1
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:PutItem
          Resource:
            - arn:aws:dynamodb:ap-northeast-1:741233137755:table/MonthlyBillingBatchResults
        - Effect: Allow
          Action:
            - states:StartExecution
          Resource:
            - "arn:aws:states:ap-northeast-1:741233137755:stateMachine:BillingBatchStateMachine"
functions:
  getBillingUsers:
    handler: handlers/getBillingUsers.handler
  sendPaymentRequest:
    handler: handlers/sendPaymentRequest.handler
  saveBatchResult:
    handler: handlers/saveBatchResult.handler
  paymentMockServer:
    handler: handlers/paymentMockServer.handler
    events:
      - http:
          path: api/v1/payments
          method: post
stepFunctions:
  stateMachines:
    billingBatch:
      name: BillingBatchStateMachine
      definition:
        StartAt: GetBillingUsers
        States:
          GetBillingUsers:
            Type: Task
            Resource:
              Fn::GetAtt: [getBillingUsers, Arn]
            Next: ProcessUsers
          ProcessUsers:
            Type: Map
            ItemsPath: "$.users"
            Iterator:
              StartAt: SendPaymentRequest
              States:
                SendPaymentRequest:
                  Type: Task
                  Resource:
                    Fn::GetAtt: [sendPaymentRequest, Arn]
                  Catch:
                    - ErrorEquals: ["States.ALL"]
                      Next: Error
                  Next: Success
                Success:
                  Type: Pass
                  Result: { "status": "success" }
                  End: true
                Error:
                  Type: Pass
                  Result: { "status": "error" }
                  End: true
            Next: SaveBatchResult
          SaveBatchResult:
            Type: Task
            Resource:
              Fn::GetAtt: [saveBatchResult, Arn]
            Next: Finished
          Finished:
            Type: Succeed
resources:
  Resources:
    MonthlyBillingBatchResultsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: MonthlyBillingBatchResults
        AttributeDefinitions:
          - AttributeName: Id
            AttributeType: S
        KeySchema:
          - AttributeName: Id
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 5
          WriteCapacityUnits: 5
plugins:
  - serverless-step-functions
